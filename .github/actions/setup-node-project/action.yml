name: Setup Node project
description: Node 24 (native TypeScript execution) + npm ci
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 24
        cache: npm
    - run: npm ci
      shell: bash
